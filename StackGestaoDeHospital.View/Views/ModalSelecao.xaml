<Window x:Class="StackGestaoDeHospital.View.ModalSelecao"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selecionar"
        Height="220"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#111111"
    >

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TituloTextBlock"
                   Grid.Row="0"
                   Text="Selecione uma pessoa"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <ComboBox x:Name="ComboSeletor"
                  Grid.Row="1"
                  Height="36"
                  Background="White"
                  Foreground="Black"
                  Margin="0,0,0,16"/>

        <Button Grid.Row="3"
                Content="Confirmar"
                Width="100"
                Height="32"
                HorizontalAlignment="Right"
                Click="Confirmar_Click"/>
    </Grid>
</Window>